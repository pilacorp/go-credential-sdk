[package]
name = "zkryptium-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
hex = "0.4.3"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
zkryptium = { version = "0.6.2", default-features = false, features = ["bbsplus"] }
